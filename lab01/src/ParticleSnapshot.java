
public class ParticleSnapshot {
  private Particle particle;
  private double mass;
  
  public ParticleSnapshot(Particle particle, double mass) {
	  //this.particle = new Particle(particle.getPosition().x, particle.getPosition().y, particle.getPosition().z);
	  this.mass = mass;
  }
}